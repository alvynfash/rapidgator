name: rapidgator
description: "An easy to use Rapidgator API client for Dart and Flutter."
version: 0.0.1
# publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.8.0+1
  equatable: ^2.0.7
  logger: ^2.5.0

dev_dependencies:
  mockito: ^5.4.5
  build_runner:
  test: ^1.25.5
  very_good_analysis: ^5.1.0
